# Статус проекта

Дата актуализации: 24.08.2026.

## Цель

Перенести `numerologpro.ru` с Ghost на статический сайт из HTML, CSS, JavaScript и локальных ресурсов, опубликовать его через GitHub Pages и после полного QA подключить существующий домен.

Итоговый сайт представляет «Тихий Дом» как зонтичный бренд и предоставляет бесплатные точки входа в отдельные Telegram-боты продуктов.

## Текущий гейт

Read-only получение и первичный аудит HTML исходных страниц завершены. Локальные временные снимки прошли автоматическую проверку и человеческий визуальный QA.

Текущий гейт - стабилизация опубликованного статического migration baseline: локализация обязательных ресурсов, удаление доказанных Ghost-остатков и технический QA GitHub Pages preview.

## Репозиторий

- GitHub-владелец: `dmitya30`.
- Репозиторий: `https://github.com/dmitya30/numerologpro`.
- Локальный путь: `~/projects/numerologpro`.
- Основная ветка: `main`.
- Первый коммит: `af775211841937ddfa5244262022d7c1d9776f6c` (`docs: establish project execution contract`).
- Базовая документация зафиксирована коммитом `d75faaa7b4e4a73c193768f9b4e5da02c1b931a7`.
- Инструмент исходного аудита зафиксирован коммитом `2ce304d501b55c7cb4fa6e7a34dbaa2df5028e79`.
- GitHub CLI локально не установлен.
- `origin` настроен, локальный и удалённый `main` синхронизированы.

## Обязательные маршруты

- `/`;
- `/elyor/`;
- `/quantocode/`;
- `/oracle/`.

Маршрут `/about/` остаётся на отдельном контентном гейте и пока не входит в обязательную навигацию или sitemap.

## Не переносится

- блог;
- статья блога;
- RSS;
- архив автора;
- архив тега;
- Ghost Search;
- Ghost Portal;
- Ghost members;
- Ghost comments;
- Ghost Admin;
- `/privacy/`;
- `/consent/`;
- `/oferta/`;
- тарифы, цены, кнопки покупки и сведения об оплате.

## Подтверждённое состояние исходного сайта

Публичный аудит 23.08.2026 подтвердил:

- сайт работает на Ghost 5.130;
- корневой домен и `www` указывают на IPv4 `5.183.191.200`;
- DNS обслуживается `ns1.reg.ru` и `ns2.reg.ru`;
- публичной AAAA-записи не обнаружено;
- публичных MX, TXT и CAA-записей для корня домена не обнаружено;
- Ghost sitemap содержит страницы, запись блога, архив автора и архив тега;
- текущий сайт публикует RSS;
- текущая 404 является служебной англоязычной страницей Ghost;
- текущая главная и `/quantocode/` существенно пересекаются по назначению и содержанию;
- текущий сайт использует Ghost Source, Code Injection, Ghost Portal, Ghost Search, member attribution, comment counts, Яндекс Метрику и Google Analytics;
- текущий публичный контент использует локальные Ghost-изображения, локальные шрифты и внешние ресурсы.

Эти сведения являются предварительной публичной инвентаризацией. Перед использованием конкретного файла, URL или настройки требуется повторная фактическая проверка.

## Завершённый первичный аудит страниц

- Через системный `curl` получены HTML-снимки `/`, `/elyor/`, `/quantocode/`, `/oracle/` и `/about/`.
- `/about/` проверена только как материал отдельного контентного гейта и не включена автоматически в scope нового сайта.
- Снимки и отчёт `audit.json` хранятся во временном игнорируемом каталоге `.work/source-audit/`.
- Проверены успешные HTTP-ответы, размеры снимков и их соответствие отчёту.
- Выполнен человеческий визуальный QA локальных снимков.
- Зафиксировано наличие коммерческих и Ghost-служебных маркеров, которые нельзя переносить автоматически.
- В репозитории находится воспроизводимый инструмент `scripts/audit-source.py`; временные снимки в Git не добавляются.
- Санитизированный manifest и человекочитаемая сводка исходных страниц подготовлены в отслеживаемом каталоге `migration-audit/`.
- Полная рекурсивная инвентаризация ресурсов из CSS ещё не выполнена.

## Инфраструктура и срок

Текущий VPS оплачен до 05.09.2026. Он остаётся временным источником и fallback до завершения публикации, DNS cutover и production QA.

Production-перенос должен быть завершён до окончания оплаченного периода с запасом на DNS, HTTPS, проверку и rollback.

## Текущие ограничения

- GitHub Pages настроен и доступен по `https://dmitya30.github.io/numerologpro/`.
- Четыре обязательные HTML-страницы и подтверждённые runtime-ресурсы перенесены в репозиторий.
- Фактический полный список runtime-ресурсов обязательных страниц ещё не зафиксирован.
- Решение по `/about/` ещё не принято.
- Яндекс Метрика и Google Analytics сохраняются; Ghost-служебный Code Injection удаляется.
- DNS не изменялся.
- VPS и Ghost не изменялись.
- GitHub Pages preview доступен для технического и человеческого визуального QA.
