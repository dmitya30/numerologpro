# Рабочий журнал

Этот журнал фиксирует промежуточные checkpoint-коммиты, незавершённые реализации, ошибки автоматизации и следующий безопасный шаг. Он дополняет `OPERATIONS.md` и нужен для восстановления работы после сжатия или потери диалога.

## 25.08.2026 — Сохранение контекста редизайна главной

- Базовый migration-коммит: `922ca7b21d4e10c76ba5f9d3b581bc0f8d25188b`.
- Изображения новой главной сохранены коммитом `3405862a1c2d7d14767a26f832a43c69c74b2a2a`.
- Утверждено направление «Тихий Дом как живой цифровой портал».
- Утверждены структура новой главной, отдельная пользовательская 404 и границы редизайна.
- Ошибочный генератор сохранён без исправлений в `.work/wrong-script.txt` как первичный артефакт восстановления.
- Генератор остановился до создания `index.html`, `404.html`, `assets/css/home.css` и `assets/js/home.js`.
- Причина остановки: фактические количества маркеров аналитики `GA=1`, `YM=2` ошибочно сравнивались с жёстко заданными значениями `GA=4`, `YM=8`.
- Следующий шаг: исправлять и выполнять работу небольшими checkpoint-коммитами, не изменяя первичный ошибочный скрипт.

Статус: контекст сохранён; реализация новой главной ещё не начата.

## 25.08.2026 — Checkpoint CSS и JavaScript новой главной

- Из первичного ошибочного скрипта восстановлены `assets/css/home.css` и `assets/js/home.js`.
- Файлы сохранены до расширенного QA.
- Следующий шаг: восстановить новую главную и пользовательскую 404.

## 25.08.2026 — Checkpoint новой главной и 404

- Из сохранённого первичного скрипта восстановлены новая `index.html` и пользовательская `404.html`.
- Фактические блоки Google Analytics и Яндекс Метрики перенесены из предыдущей главной без жёсткой проверки ошибочных количеств 4/8.
- Страницы сохранены в Git до расширенного QA.
- Следующий шаг: выполнить только необходимые проверки и исправить подтверждённые ошибки отдельным patch-коммитом.

## 25.08.2026 - Checkpoint логотипа и ASCII-дефисов

- Фирменный логотип перенесён из Ghost-каталога в `assets/images/brand/logo.svg`.
- Искусственный CSS-знак главной заменён существующим фирменным логотипом.
- Ссылки на логотип обновлены на главной и продуктовых страницах.
- Unicode-тире и Unicode-дефисы в пяти публикуемых HTML-файлах заменены на ASCII-дефис.
- Следующий шаг: аудит фактически используемых ресурсов перед переносом и удалением Ghost-наследия.

## 25.08.2026 - Checkpoint аудита runtime-ресурсов

- В `migration-audit/runtime-assets.md` сохранены фактические ссылки обязательных страниц и список файлов из `assets/`, `content/` и `public/`.
- Следующий шаг: сформировать точную карту переноса и удалить только ресурсы без runtime-ссылок.

## 25.08.2026 - Checkpoint упрощения структуры ресурсов

- Используемые изображения перенесены из Ghost-каталога `content/` в `assets/images/brand/` и `assets/images/products/`.
- `screen.css` и `source.js` перенесены из `assets/built/` в обычные каталоги CSS и JavaScript.
- `cards.min.css` и `cards.min.js` перенесены из `public/` в `assets/css/` и `assets/js/`.
- Подтверждённо неиспользуемый `hero-2.webp` удалён.
- Ссылки пяти публикуемых HTML-файлов обновлены.
- Следующий шаг: проверить перенесённые ресурсы и определить, нужны ли `site.*` и `cards.min.*` фактически.

## 25.08.2026 - Patch относительных ресурсов и устаревших изображений

- Внутренние ресурсы главной и трёх продуктовых страниц переведены на относительные пути для GitHub Project Pages и production.
- `overview.jpg` удалён: он использовался только старым Ghost author Schema.org и ссылался на исключённый `/author/`.
- Старые `og-main.jpg` и `social-main.png` удалены.
- Временным OG/Twitter-изображением продуктовых страниц назначен актуальный `og-quiet-house.jpg`; отдельные продуктовые OG можно подготовить при их редизайне.
- Видимый ресурс `elyor.svg` сохранён и переведён на относительный путь.
- Следующий шаг: удалить оставшиеся доказанные Ghost-зависимости и неиспользуемые runtime-файлы.


## 25.08.2026 - Patch фирменной типографики главной

- Заголовки главной переведены с Inter на фирменный шрифт Lora.
- Основной текст и интерфейсные элементы сохранены в Inter.
- Lora сохранён локально в `assets/fonts/`; внешняя зависимость главной от Google Fonts не добавлялась.
- SVG-логотип не изменялся: он содержит готовую векторную графику и не зависит от браузерного шрифта.
- Следующий шаг: удаление оставшихся подтверждённых Ghost-зависимостей и неиспользуемых runtime-файлов.

## 25.08.2026 - Patch удаления неиспользуемого Ghost Cards runtime

- Проверка DOM трёх продуктовых страниц подтвердила отсутствие элементов с классами `kg-*`.
- Удалены неиспользуемые `assets/css/cards.min.css` и `assets/js/cards.min.js`.
- Ссылки на Ghost Cards runtime удалены из `/elyor/`, `/quantocode/` и `/oracle/`.
- `site.css` и `site.js` пока сохранены: продуктовые страницы используют классы существующей оболочки и навигации.
- Следующий шаг: локализовать оставшиеся шрифтовые зависимости и продолжить очистку Ghost-оболочки.

## 25.08.2026 - Patch шрифтов, site.js и мобильного hero

- Все Google Fonts зависимости удалены из трёх продуктовых страниц.
- Lora и Inter подключены из локальных `assets/css/` и `assets/fonts/`.
- `site.js` сокращён до логики существующей навигации.
- Удалены неиспользуемые Ghost lightbox, pagination, PhotoSwipe и imagesLoaded.
- Мобильное hero-изображение главной масштабируется по ширине без боковой обрезки.
- Текст мобильного hero размещён ниже изображения и не перекрывает его визуальную часть.
- Дизайн и тексты продуктовых страниц не изменялись.
- Следующий шаг: визуальный QA главной и проверка оставшейся Ghost CSS-оболочки.

## 25.08.2026 - Patch композиции мобильного hero

- Мобильное изображение сохранено целиком и масштабируется по ширине без боковой обрезки.
- Название секции размещено в верхнем левом тёмном поле ниже логотипа.
- Заголовок и подзаголовок возвращены на нижнее тёмное поле изображения.
- Размер мобильного подзаголовка уменьшен до 16px.
- Кнопки и примечание размещены за нижней границей изображения.
- Исходный файл `hero-mobile.webp` не изменялся.
- Следующий шаг: визуальный QA композиции на узком мобильном экране.


## 25.08.2026 - Checkpoint единой CSS/JS-основы

- Главная переведена на финальные `assets/css/site.css` и `assets/js/site.js`.
- Локальные правила Lora объединены со стилями главной в `site.css`.
- `home.css`, `lora.css` и `home.js` удалены как отдельные runtime-файлы.
- Прежний Ghost CSS временно сохранён как `legacy-product.css` только для трёх продуктовых страниц.
- Защитная проверка позволяет единому `site.js` работать и на главной без Ghost DOM.
- Контент, аналитика и визуальная композиция страниц на этом шаге не изменялись.
- Следующий шаг: семантическая пересборка и лёгкий редизайн продуктовых страниц без Ghost-классов.


## 25.08.2026 - Checkpoint входных данных очистки продуктовых страниц

- В `migration-audit/product-cleanup-inputs.md` зафиксированы текущие заголовки, Telegram CTA и runtime-ресурсы трёх продуктовых страниц.
- Зафиксированы количества Ghost-маркеров, inline CSS/JS, ссылок на временный `legacy-product.css` и коммерческих формулировок.
- Сырые HTML, код аналитики, cookies, credentials и инфраструктурные данные в отчёт не копировались.
- Опубликованные HTML, CSS и JavaScript на этом шаге не изменялись.
- Следующий шаг: пересобрать `/quantocode/` на семантическом HTML и общей дизайн-системе, сохранив подтверждённый CTA, SEO и аналитику.

## 25.08.2026 - Checkpoint общей дизайн-системы продуктовых страниц

- В `assets/css/site.css` добавлены изолированные стили для новых продуктовых страниц.
- Подготовлены общие hero, карточки, информационные секции, шаги, CTA и адаптивные состояния.
- Для КвантоКода, Mystic Oracle и ЭЛИОР предусмотрены отдельные цветовые акценты без дополнительных CSS-файлов.
- Существующие HTML и JavaScript на этом шаге не изменялись.
- Следующий шаг: перевести `/quantocode/` с Ghost-разметки на чистый semantic HTML.

## 25.08.2026 - Коррекция границ миграции продуктовых страниц

- Неутверждённая продуктовая дизайн-система удалена из `assets/css/site.css`.
- HTML, контент, визуал, аналитика и CTA продуктовых страниц не изменялись.
- Следующий шаг: перенести существующие стили `/quantocode/`, `/oracle/` и `/elyor/` в единый `site.css` без изменения дизайна и разметки.
- `legacy-product.css` сохраняется до завершения переноса и визуального QA всех трёх страниц.

## 26.08.2026 - Восстановление продуктового migration baseline

- Не прошедшие визуальный QA изменения `0eebe9a`, `f1f7a1e` и `0abcf49` сняты точечным восстановлением файлов из дерева `c153c54`.
- Исторические пробельные строки в восстановленных HTML признаны неблокирующими, поскольку файлы проверены на точное совпадение с подтвержденным Git-деревом.
- Соответствующее обязательное правило добавлено в `AGENTS.md`.
- `legacy-product.css`, `site.js` и исходные снимки `.work/static-baseline/` не изменялись.
- Следующий шаг: послойное удаление Ghost runtime без одновременного изменения CSS и продуктовой разметки.

## 26.08.2026 - Удаление мёртвого Ghost members workaround

- Подтверждено, что внешний Ghost Portal, Search, comments и members runtime уже отсутствует на трех продуктовых страницах.
- Из `/quantocode/`, `/oracle/` и `/elyor/` удален оставшийся inline-скрипт, предназначенный только для отключения Ghost member attribution.
- Другие script-блоки, аналитика, Telegram URL, видимый текст, inline CSS, разметка и визуальное оформление не изменялись.
- Следующий шаг: аудит Ghost-классов по фактическому использованию в `legacy-product.css` перед заменой header/footer и content shell.

## 26.08.2026 - Удаление дублирующих Ghost article headers

- Из `/quantocode/` и `/elyor/` удалены дублирующие Ghost article headers с повторными заголовками и excerpt.
- На `/oracle/` такого блока не было, поэтому страница не изменялась.
- На каждой продуктовой странице оставлен ровно один продуктовый `h1`.
- Содержимое `qk-wrap`, `oracle-page-wrapper` и `el-wrap`, CSS, JavaScript, SEO, аналитика и Telegram URL не изменялись.
- Ghost content shell пока сохранен, поскольку его CSS-контекст необходимо переносить отдельно без визуальной регрессии.
- Следующий шаг: безопасная замена классов header/footer с одновременным переносом только фактически необходимых CSS-правил.

## 26.08.2026 - Объединение локальных Inter font-face с product CSS

- Локальные `@font-face` из `assets/css/inter.css` добавлены в уже подключенный `legacy-product.css` без изменения остальных CSS-правил.
- Из `/quantocode/`, `/oracle/` и `/elyor/` удалено отдельное подключение `inter.css`.
- HTML, кроме удаленных link-тегов, inline CSS, JavaScript и продуктовые wrapper не изменялись.
- Файл `inter.css` физически сохранен до финального визуального QA и проверки отсутствия ссылок со всего сайта.
- Следующий шаг: визуальный QA шрифтов, затем перенос product CSS в отдельный от главной финальный bundle без снятия Ghost content shell.

## 26.08.2026 - Сохранение существующих html-классов product pages

- На трех продуктовых страницах destructive-присваивание `document.documentElement.className` заменено на `classList.add`.
- Текущий визуальный класс `has-dark-text` или `has-light-text` сохраняется, но последующие уникальные классы страниц больше не будут удаляться runtime-кодом.
- CSS, видимый текст, аналитика и продуктовая разметка не изменялись.
- Следующий шаг: добавить стабильные уникальные классы страниц и использовать их для безопасной изоляции переносимых inline CSS-правил.

## 26.08.2026 - Полная пересборка оболочки продуктовых страниц

- Три продуктовые страницы получили чистую статическую body-оболочку без Ghost navigation, footer и внешних Ghost-контейнеров.
- Содержимое qk-wrap, oracle-page-wrapper и el-wrap сохранено и перенесено непосредственно в main.
- Добавлен общий статический header с логотипом, продуктовой Telegram-кнопкой и визуальными вариантами для трех продуктов.
- Страницы переведены с legacy-product.css на products.css.
- SEO, аналитика, существующие продуктовые CTA и внутреннее содержимое wrapper сохранены.
- Проведён локальный визуальный QA трех страниц; header скорректирован по опубликованному визуальному baseline.
- Следующий шаг: удалить из products.css неиспользуемые Ghost-селекторы и перенести оставшиеся необходимые inline CSS-правила без изменения внешнего вида.
## 26.08.2026 - Удаление Ghost theme CSS из product bundle

- Из products.css удален неиспользуемый CSS bundle темы Ghost Source.
- Сохранены минимальный reset, базовые переменные, локальные Inter font-face и подтвержденные стили чистой product shell.
- HTML, SEO, аналитика, Telegram CTA, wrapper-разметка и embedded product CSS не изменялись.
- Следующий шаг: локальный визуальный QA, затем перенос embedded product CSS в products.css.

## 27.08.2026 - CSS продуктовых страниц вынесен из HTML

- Четыре inline style-блока каждой продуктовой страницы перенесены во внешние файлы `product-quantocode.css`, `product-oracle.css` и `product-elyor.css`.
- Общий reset и проверенный header остаются в `assets/css/products.css`.
- Исправлен общий селектор `*`, `*::before`, `*::after`.
- DOM, header, main-wrapper, JavaScript, аналитика и Telegram-ссылки не изменялись.
- Локальный визуальный QA `/quantocode/`, `/oracle/` и `/elyor/` пройден.

## 27.08.2026 - Удалены Ghost-селекторы из product CSS

- Из `product-quantocode.css`, `product-oracle.css` и `product-elyor.css` удалены селекторы `.gh-*` и Ghost-only правила.
- В смешанных групповых правилах сохранены действующие не-Ghost селекторы.
- HTML, общий `products.css`, header, main-wrapper, JavaScript и ссылки не изменялись.
- После очистки выполнен локальный визуальный QA всех трёх продуктовых страниц; изменений UI не обнаружено.

## 27.08.2026 - Удалён мёртвый JavaScript Ghost-оболочки

- Из трёх продуктовых страниц удалён неиспользуемый расчёт Ghost-классов контрастности.
- Удалён скрипт, создававший CTA внутри отсутствующей Ghost-навигации.
- Удалён дублирующий общий обработчик Quantocode CTA и отсутствующего блока pricing.
- Продуктовые обработчики CTA, аналитика, HTML, CSS, header и видимый контент сохранены.
- Следующий шаг: удалить оставшиеся обращения JavaScript к отсутствующей Ghost-навигации, сохранив продуктовый CTA tracking.

## 27.08.2026 - Product tracking отвязан от Ghost-навигации

- В продуктовых tracking-блоках удалены обращения к `.gh-navigation-menu`, `.gh-navigation-actions` и `.gh-navigation-brand`.
- Определение header CTA переведено на существующую статическую оболочку `.product-header`, `.product-header__inner` и `.product-header__logo`.
- Названия событий, Telegram URL, вызовы Яндекс Метрики и Google Analytics сохранены.
- HTML-структура, CSS, видимый контент и количество script-блоков не изменялись.
- Следующий шаг: провести глобальную инвентаризацию оставшихся Ghost-маркеров и runtime-ресурсов во всех публикуемых страницах.

## 27.08.2026 - Исправлено скрытие статических product headers

- После перевода tracking-селекторов с Ghost-разметки на статический header обнаружена регрессия: legacy-скрипт применял `display: none` к `.product-header` и `.product-header__inner`.
- Из трёх продуктовых страниц удалена мёртвая логика скрытия навигации и динамического создания дублирующего CTA.
- Существующая статическая структура header и её стили сохранены; Ghost-разметка не восстанавливалась.
- Продуктовые события аналитики и делегированный tracking кликов по Telegram CTA сохранены.
- Локальный визуальный QA `/quantocode/`, `/oracle/` и `/elyor/` пройден: headers снова отображаются корректно.
- Следующий шаг: глобальный read-only аудит Ghost-маркеров, runtime-ресурсов, CTA и metadata всех публикуемых страниц.

## 27.08.2026 - Удалён неиспользуемый Ghost navigation runtime

- Удалён legacy-product.css: ни одна из пяти публикуемых HTML-страниц больше не подключает этот файл.
- Из site.js удалена функция dropdown, обслуживавшая только отсутствующую Ghost-навигацию.
- Живая логика появления блоков и лёгкого движения hero-изображения главной сохранена без изменений.
- Product HTML, header, CTA, аналитика, metadata и продуктовые стили не изменялись.
- Следующий шаг: удалить оставшиеся текстовые Ghost-маркеры из product CSS без изменения действующих правил.

## 27.08.2026 - Удалены текстовые Ghost-маркеры из product CSS

- Из трех product CSS удалены неиспользуемые объявления --ghost-accent-color.
- Удалены комментарии об уже отсутствующих gh-content и старых заголовках.
- Комментарий общей product CSS-основы переименован без изменения правил.
- Действующие CSS-декларации, HTML, header, CTA, JavaScript и аналитика не изменялись.
- Следующий шаг: повторить глобальный read-only аудит runtime-ресурсов и metadata.

## 27.08.2026 - Закрыты блокеры глобального static baseline audit

- На страницах КвантоКода и ЭЛИОР удалены только дубли Open Graph и Twitter metadata; основные SEO-теги сохранены.
- С трех product pages удалена битая ссылка на lora.css; локальные Lora font-face и файлы шрифта сохранены.
- Удалены ссылки на исключенные решением D-006 юридические маршруты.
- Удален неиспользуемый inter.css, определения которого уже находятся в products.css.
- Header, CTA, аналитика, canonical, Schema и основной контент не изменялись.
- Следующий шаг: контрольный read-only metadata/runtime audit и проверка Schema.org.

## 27.08.2026 - Удалены ошибочные Schema.org-хвосты КвантоКода

- Из страниц Oracle и ЭЛИОР удалены скопированные Ghost JSON-LD-блоки Organization и WebSite с alternateName КвантоКод.
- Продуктовые Article, WebPage и SoftwareApplication Schema.org сохранены.
- Видимый контент, CSS, header, CTA, аналитика и Telegram-ссылки не изменялись.
- Следующий шаг: удалить подтвержденные коммерческие формулировки и повторить глобальный read-only audit.

## 27.08.2026 - Восстановлена Lora на product pages

- Lora font-face для начертаний 500 и 600 добавлены в общий products.css из действующего site.css.
- На трех product pages обновлена версия products.css для сброса браузерного кеша.
- На страницах Oracle и ЭЛИОР удалены явные упоминания способа оплаты и цены.
- Главная страница, CTA, аналитика, Schema.org и Telegram-ссылки не изменялись.
- Следующий шаг: глобальный read-only static audit.

## 27.08.2026 - Исправлено начертание Lora на product pages

- Для italic-типографики product pages добавлены upright-алиасы локальной Lora без синтетического наклона.
- Версия общего products.css обновлена до v=3 для сброса браузерного кеша.
- Тексты, DOM, CTA, аналитика и Schema.org не изменялись.
- Следующий шаг: визуальная проверка Lora и глобальный read-only static audit.

## 27.08.2026 - Восстановлена настоящая Lora Italic и sticky product header

- Ошибочные upright-алиасы удалены; для product pages локализована настоящая Lora Italic 500-600.
- Общий product header переведен в sticky-режим и остается наверху при прокрутке.
- Версия products.css обновлена до v=4 для сброса браузерного кеша.
- Контент, DOM, CTA, аналитика и Schema.org не изменялись.
- Следующий шаг: визуальный QA и глобальный read-only static audit.

## 27.08.2026 - Удалены публикуемые `.work`-артефакты

- Из актуальной ветки удалены 48 отслеживаемых временных файлов `.work/`.
- Каталог добавлен в `.gitignore`; локальные копии сохранены как рабочие артефакты.
- Публичный URL старого ошибочного скрипта проверен и возвращает 404.
- Следующий шаг: глобальный read-only static audit.

## 27.08.2026 - Завершены абсолютные SEO URL и GA bootstrap

- Коммит `8682cebb672d575106c6d9851a8c5dff43095943` перевёл social images и Schema.org logo на абсолютные URL `https://numerologpro.ru/`.
- На главной и 404 добавлены `dataLayer`, функция `gtag`, событие `js` и конфигурация `G-EEJ6Z1C216`.
- Product pages уже содержали корректную инициализацию GA и не получили дублирующих блоков.
- Видимый контент, CSS, CTA и Яндекс Метрика не изменялись.
- Следующий шаг: синхронизация постоянной документации перед аудитом product CSS.

## 27.08.2026 - Синхронизирована документация статического baseline

- `STATUS.md` обновлён до фактического migration gate.
- В `TODO.md` закрыты выполненные задачи metadata, Ghost runtime, ресурсов, шрифтов и коммерческого контента.
- `OPERATIONS.md` дополнен существенными операциями стабилизации и удаления `.work/`.
- Добавлен санитизированный текущий отчёт `migration-audit/static-baseline-2026-08-27.md`.
- `AGENTS.md` проверен и не изменён: его правила `.work/` и `migration-audit/` уже соответствуют текущему состоянию.
- Следующий шаг: read-only аудит фактического использования product CSS без изменений файлов.

## 27.08.2026 - Консервативная очистка мёртвых product CSS selectors

- Read-only аудит выполнен на checkpoint `568fc7045524d0268423c0ed2a9c4c956ce6f7bc`.
- Из трёх product-specific CSS удалены только selector branches с доказанно отсутствующими class, id или theme-state tokens.
- Смешанные selector groups сохранены с удалением только мёртвых ветвей.
- Действующие глобальные правила, product selectors, общий `products.css`, шрифты, DOM, CTA и аналитика не изменялись.
- Версии product-specific CSS обновлены с `v=1` до `v=2`.
- Следующий шаг: визуальный QA, затем аудит оставшихся активных глобальных правил и variables.

## 28.08.2026 - Завершена очистка остаточных dead product selectors

- После первого cleanup обнаружена неполная обработка вложенных и смешанных selectors.
- Удалены остаточные `dark-mode`, `nav-current`, `site-main`, theme-button и `body.page-*` rules.
- Активные product selectors и визуальное оформление сохранены.
- Версии product-specific CSS обновлены до `v=3`.
- Визуальный QA предыдущего cleanup подтверждён пользователем как PASS.
- Следующий шаг: аудит оставшихся global rules и custom properties.

## 28.08.2026 - Консолидация общего product CSS

- Read-only аудит подтвердил идентичность глобального CSS-префикса трёх product pages.
- Общий префикс перенесён в `assets/css/products.css` без изменения нормализованного effective CSS.
- Product-specific CSS теперь начинается с фактических product wrapper selectors.
- Версия общего CSS обновлена до `v=5`, product-specific CSS - до `v=4`.
- Неиспользуемые variables и глобальные selector branches пока не удалялись.
- Следующий шаг: визуальный QA, затем отдельная точечная очистка общего блока.

## 28.08.2026 - Удаление Ghost theme и unused global product CSS

- После консолидации общего CSS пользователь подтвердил визуальный QA трёх product pages: PASS.
- Удалены неиспользуемый `prefers-color-scheme` override и остаточные theme-state правила Ghost.
- Удалена 21 неиспользуемая custom property и дублирующий override `--color-primary-text`.
- Удалены отсутствующие element selector branches, пустой mobile media block и исторические комментарии.
- Активные layout, typography, links и focus styles сохранены.
- Версия общего `products.css` обновлена с `v=5` до `v=6`.
- Следующий шаг: визуальный QA трёх product pages.

## 28.08.2026 - Product CSS cleanup gate завершён

- После удаления Ghost theme и unused global CSS повторный визуальный QA `/quantocode/`, `/oracle/` и `/elyor/` пройден: PASS.
- Подтверждены фон, sticky header, типографика, CTA и основные content sections.
- Гейт очистки product CSS завершён на техническом baseline `4a3666dc27d9e14955e6d772b9de94aae85b64ec`.
- Следующий шаг: добавить воспроизводимые автоматические проверки HTML, metadata, ссылок и runtime-ресурсов.

## 28.08.2026 - Добавлены воспроизводимые static-site проверки

- Добавлен `scripts/check-static-site.py` без внешних Python-зависимостей.
- Проверяются HTML structure, metadata, JSON-LD, canonical, аналитика и Telegram CTA.
- Проверяются внутренние ссылки, локальные HTML/CSS runtime-ресурсы, robots.txt и sitemap.xml.
- Проверяется отсутствие Ghost runtime, theme switching, исключённых legal routes и коммерческих markers.
- Первый полный запуск проверок: PASS.
- Следующий шаг: browser-level QA console, network, keyboard focus и horizontal scroll.

## 28.08.2026 - GitHub Pages draft browser QA завершён

- На checkpoint `f55a17e157c53ff258212d2b76091d2ec2d554f9` повторно выполнен `scripts/check-static-site.py`: PASS, 274 проверки.
- Обязательные маршруты project preview возвращают HTTP 200; пользовательская 404 возвращает HTTP 404.
- Выполнен полный browser-level QA desktop и mobile layout, navigation, CTA, footer, images и fonts: PASS.
- Проверены horizontal scroll, keyboard navigation, focus states, browser console и network: PASS.
- Подтверждено отсутствие runtime-запросов к Ghost и VPS; DNS, VPS и Ghost не изменялись.
- Следующий шаг: зафиксировать поведение `/about/` и остальных legacy URL перед подготовкой DNS cutover.

## 28.08.2026 - Поведение `/about/` и legacy URL зафиксировано

- Read-only аудит подтвердил HTTP 200 четырёх обязательных маршрутов и пользовательскую 404 исключённых маршрутов в GitHub Pages preview.
- Первоначальная проверка Ghost markers дала ложное срабатывание из-за включения `scripts/` и `__pycache__/`; повторная проверка только публикуемых HTML, CSS и JavaScript прошла.
- `/about/` утверждена к переносу: со старого VPS берётся только текст, страница собирается заново в визуальной системе нового сайта.
- Пункт `/about/` не добавляется в навигацию главной; после реализации маршрут добавляется в sitemap.
- Legal pages, blog, post, author/tag archives, RSS и Ghost-служебные маршруты не переносятся и остаются на пользовательской 404.
- Старый VPS не изменялся и остаётся read-only источником до DNS cutover.
- Следующий шаг: извлечь текст `/about/` и реализовать новую статическую страницу.

## 28.08.2026 - Checkpoint новой `/about/`

- Со старого VPS извлечено только текстовое содержание `/about/`.
- Созданы `about/index.html` и `assets/css/about.css` в визуальной системе новой главной.
- Ghost-разметка, старые стили, Portal, legal links, blog link, коммерческие блоки и планы старого портала не перенесены.
- `/about/` не добавлена в навигацию главной.
- Sitemap и воспроизводимый checker пока не изменялись: это отдельный checkpoint после публикации и визуального QA.
- Следующий шаг: проверить `/about/` в GitHub Pages preview на desktop и mobile.

## 28.08.2026 - Гейт `/about/` завершён

- Новая статическая `/about/` опубликована в GitHub Pages preview и прошла визуальный QA.
- Удалена подпись редакции как ненужный остаток блоговой модели старого Ghost-сайта.
- `/about/` добавлена в sitemap и воспроизводимый static-site checker.
- Проверки страницы включают HTML structure, metadata, canonical, analytics, JSON-LD, Telegram CTA, внутренние ссылки и локальные ресурсы.
- Страница не добавлена в навигацию главной.
- Отдельный `about.css` остаётся временным page-specific слоем до общей дедупликации CSS после редизайна product pages.
- Следующий шаг: подготовка custom domain, фиксация DNS baseline и rollback-плана без изменения DNS.

## 28.08.2026 - Редизайн трёх product pages завершён

- КвантоКод переведён на сине-золотую визуальную систему.
- Финальный проверенный Quantocode checkpoint: d8fa94618f7c2430799fc661fd8ddd80e3ac21a3.
- Mystic Oracle переведён на фиолетово-золотую систему; hero emoji заменён CSS-сферой.
- Финальный проверенный Oracle checkpoint: 0a2da77fdc6e1123971085cf3c075ddc6a1b5692.
- ЭЛИОР обновлён с сохранением SVG и основного accent #E01CBC.
- Финальный проверенный Elyor checkpoint: dd1a9f0d77e6275adf9827d74d91dae5ff6e125d.
- Визуальный QA всех трёх обновлённых product pages: PASS.
- Тексты, CTA, Telegram URL, metadata и аналитика сохранены.
- Read-only CSS-аудит подтвердил отсутствие Ghost и theme-state markers.
- В site.css остаются два оправданных reduced-motion !important.
- В Quantocode остаётся восстановительный блок из 35 !important.
- Следующий шаг: нормализация Quantocode CSS и общая CSS-консолидация.

Статус: product redesign PASS; CSS consolidation ещё не выполнена.

## 28.08.2026 - Навигация и композиция карточек главной

- Хедер главной и `/about/` переведён в fixed-режим без смещения hero-контента.
- Добавлено доступное мобильное меню с закрытием по кнопке, ссылке, клику вне панели и клавише Escape.
- Пункт «Продукты» удалён из обеих навигаций; старый `#products` заменён внутренним якорем `#paths` только для hero CTA.
- Из подписей трёх карточек удалены номера; подписи включены в компактную нижнюю текстовую композицию.
- Изображения, сигилы, продуктовые тексты, CTA, metadata, аналитика и product-page selectors не изменялись.
- Следующий шаг: визуальный QA главной и `/about/` на desktop и mobile.

## 28.08.2026 - Уточнение пропорций карточек главной

- На desktop и tablet уменьшена высота трёх карточек направлений.
- Текстовые группы и относительные центры свечения подняты выше.
- Цвета, тексты, ссылки, мобильная композиция и product pages не изменялись.
- Следующий шаг: визуальный QA карточек на desktop, tablet и mobile.

## 28.08.2026 - Финальная очистка HTML перед production

- Удалены скрытые SEO-заголовки с трёх product pages.
- Удалены legacy Article metadata, Article JSON-LD и дублирующая Schema.org-разметка КвантоКода.
- Telegram URL очищены от устаревшего ref; три workaround-скрипта удалены.
- Удалены лишние stylesheet preload, пустые legacy-комментарии и два inline-стиля выравнивания.
- Видимые тексты, CTA, дизайн, аналитика и рабочие WebPage/SoftwareApplication schemas сохранены.
- Следующий шаг: финальный browser QA перед production cutover.


## 28.08.2026 - Финальный предрелизный гейт

- Production candidate зафиксирован на commit `5ae69967647909f9d97d8d595a791a46f7b0139f`.
- CSS-консолидация, восстановление Inter, навигация главной, карточки и финальная HTML-очистка завершены.
- Static-site integrity и пользовательский browser QA: PASS.
- Повторная read-only DNS-проверка выполнена; действующий Ghost всё ещё обслуживается с `5.183.191.200`.
- Следующий шаг: удалить `migration-audit/` из публикуемой ветки, настроить custom domain в GitHub Pages и только затем изменить DNS в REG.RU.
