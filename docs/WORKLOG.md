# Рабочий журнал

Этот журнал фиксирует промежуточные checkpoint-коммиты, незавершённые реализации, ошибки автоматизации и следующий безопасный шаг. Он дополняет `OPERATIONS.md` и нужен для восстановления работы после сжатия или потери диалога.

## 25.08.2026 — Сохранение контекста редизайна главной

- Базовый migration-коммит: `922ca7b21d4e10c76ba5f9d3b581bc0f8d25188b`.
- Изображения новой главной сохранены коммитом `3405862a1c2d7d14767a26f832a43c69c74b2a2a`.
- Утверждено направление «Тихий Дом как живой цифровой портал».
- Утверждены структура новой главной, отдельная пользовательская 404 и границы редизайна.
- Ошибочный генератор сохранён без исправлений в `.work/wrong-script.txt` как первичный артефакт восстановления.
- Генератор остановился до создания `index.html`, `404.html`, `assets/css/home.css` и `assets/js/home.js`.
- Причина остановки: фактические количества маркеров аналитики `GA=1`, `YM=2` ошибочно сравнивались с жёстко заданными значениями `GA=4`, `YM=8`.
- Следующий шаг: исправлять и выполнять работу небольшими checkpoint-коммитами, не изменяя первичный ошибочный скрипт.

Статус: контекст сохранён; реализация новой главной ещё не начата.

## 25.08.2026 — Checkpoint CSS и JavaScript новой главной

- Из первичного ошибочного скрипта восстановлены `assets/css/home.css` и `assets/js/home.js`.
- Файлы сохранены до расширенного QA.
- Следующий шаг: восстановить новую главную и пользовательскую 404.

## 25.08.2026 — Checkpoint новой главной и 404

- Из сохранённого первичного скрипта восстановлены новая `index.html` и пользовательская `404.html`.
- Фактические блоки Google Analytics и Яндекс Метрики перенесены из предыдущей главной без жёсткой проверки ошибочных количеств 4/8.
- Страницы сохранены в Git до расширенного QA.
- Следующий шаг: выполнить только необходимые проверки и исправить подтверждённые ошибки отдельным patch-коммитом.

## 25.08.2026 - Checkpoint логотипа и ASCII-дефисов

- Фирменный логотип перенесён из Ghost-каталога в `assets/images/brand/logo.svg`.
- Искусственный CSS-знак главной заменён существующим фирменным логотипом.
- Ссылки на логотип обновлены на главной и продуктовых страницах.
- Unicode-тире и Unicode-дефисы в пяти публикуемых HTML-файлах заменены на ASCII-дефис.
- Следующий шаг: аудит фактически используемых ресурсов перед переносом и удалением Ghost-наследия.

## 25.08.2026 - Checkpoint аудита runtime-ресурсов

- В `migration-audit/runtime-assets.md` сохранены фактические ссылки обязательных страниц и список файлов из `assets/`, `content/` и `public/`.
- Следующий шаг: сформировать точную карту переноса и удалить только ресурсы без runtime-ссылок.

## 25.08.2026 - Checkpoint упрощения структуры ресурсов

- Используемые изображения перенесены из Ghost-каталога `content/` в `assets/images/brand/` и `assets/images/products/`.
- `screen.css` и `source.js` перенесены из `assets/built/` в обычные каталоги CSS и JavaScript.
- `cards.min.css` и `cards.min.js` перенесены из `public/` в `assets/css/` и `assets/js/`.
- Подтверждённо неиспользуемый `hero-2.webp` удалён.
- Ссылки пяти публикуемых HTML-файлов обновлены.
- Следующий шаг: проверить перенесённые ресурсы и определить, нужны ли `site.*` и `cards.min.*` фактически.

## 25.08.2026 - Patch относительных ресурсов и устаревших изображений

- Внутренние ресурсы главной и трёх продуктовых страниц переведены на относительные пути для GitHub Project Pages и production.
- `overview.jpg` удалён: он использовался только старым Ghost author Schema.org и ссылался на исключённый `/author/`.
- Старые `og-main.jpg` и `social-main.png` удалены.
- Временным OG/Twitter-изображением продуктовых страниц назначен актуальный `og-quiet-house.jpg`; отдельные продуктовые OG можно подготовить при их редизайне.
- Видимый ресурс `elyor.svg` сохранён и переведён на относительный путь.
- Следующий шаг: удалить оставшиеся доказанные Ghost-зависимости и неиспользуемые runtime-файлы.


## 25.08.2026 - Patch фирменной типографики главной

- Заголовки главной переведены с Inter на фирменный шрифт Lora.
- Основной текст и интерфейсные элементы сохранены в Inter.
- Lora сохранён локально в `assets/fonts/`; внешняя зависимость главной от Google Fonts не добавлялась.
- SVG-логотип не изменялся: он содержит готовую векторную графику и не зависит от браузерного шрифта.
- Следующий шаг: удаление оставшихся подтверждённых Ghost-зависимостей и неиспользуемых runtime-файлов.

## 25.08.2026 - Patch удаления неиспользуемого Ghost Cards runtime

- Проверка DOM трёх продуктовых страниц подтвердила отсутствие элементов с классами `kg-*`.
- Удалены неиспользуемые `assets/css/cards.min.css` и `assets/js/cards.min.js`.
- Ссылки на Ghost Cards runtime удалены из `/elyor/`, `/quantocode/` и `/oracle/`.
- `site.css` и `site.js` пока сохранены: продуктовые страницы используют классы существующей оболочки и навигации.
- Следующий шаг: локализовать оставшиеся шрифтовые зависимости и продолжить очистку Ghost-оболочки.

## 25.08.2026 - Patch шрифтов, site.js и мобильного hero

- Все Google Fonts зависимости удалены из трёх продуктовых страниц.
- Lora и Inter подключены из локальных `assets/css/` и `assets/fonts/`.
- `site.js` сокращён до логики существующей навигации.
- Удалены неиспользуемые Ghost lightbox, pagination, PhotoSwipe и imagesLoaded.
- Мобильное hero-изображение главной масштабируется по ширине без боковой обрезки.
- Текст мобильного hero размещён ниже изображения и не перекрывает его визуальную часть.
- Дизайн и тексты продуктовых страниц не изменялись.
- Следующий шаг: визуальный QA главной и проверка оставшейся Ghost CSS-оболочки.

## 25.08.2026 - Patch композиции мобильного hero

- Мобильное изображение сохранено целиком и масштабируется по ширине без боковой обрезки.
- Название секции размещено в верхнем левом тёмном поле ниже логотипа.
- Заголовок и подзаголовок возвращены на нижнее тёмное поле изображения.
- Размер мобильного подзаголовка уменьшен до 16px.
- Кнопки и примечание размещены за нижней границей изображения.
- Исходный файл `hero-mobile.webp` не изменялся.
- Следующий шаг: визуальный QA композиции на узком мобильном экране.


## 25.08.2026 - Checkpoint единой CSS/JS-основы

- Главная переведена на финальные `assets/css/site.css` и `assets/js/site.js`.
- Локальные правила Lora объединены со стилями главной в `site.css`.
- `home.css`, `lora.css` и `home.js` удалены как отдельные runtime-файлы.
- Прежний Ghost CSS временно сохранён как `legacy-product.css` только для трёх продуктовых страниц.
- Защитная проверка позволяет единому `site.js` работать и на главной без Ghost DOM.
- Контент, аналитика и визуальная композиция страниц на этом шаге не изменялись.
- Следующий шаг: семантическая пересборка и лёгкий редизайн продуктовых страниц без Ghost-классов.


## 25.08.2026 - Checkpoint входных данных очистки продуктовых страниц

- В `migration-audit/product-cleanup-inputs.md` зафиксированы текущие заголовки, Telegram CTA и runtime-ресурсы трёх продуктовых страниц.
- Зафиксированы количества Ghost-маркеров, inline CSS/JS, ссылок на временный `legacy-product.css` и коммерческих формулировок.
- Сырые HTML, код аналитики, cookies, credentials и инфраструктурные данные в отчёт не копировались.
- Опубликованные HTML, CSS и JavaScript на этом шаге не изменялись.
- Следующий шаг: пересобрать `/quantocode/` на семантическом HTML и общей дизайн-системе, сохранив подтверждённый CTA, SEO и аналитику.

## 25.08.2026 - Checkpoint общей дизайн-системы продуктовых страниц

- В `assets/css/site.css` добавлены изолированные стили для новых продуктовых страниц.
- Подготовлены общие hero, карточки, информационные секции, шаги, CTA и адаптивные состояния.
- Для КвантоКода, Mystic Oracle и ЭЛИОР предусмотрены отдельные цветовые акценты без дополнительных CSS-файлов.
- Существующие HTML и JavaScript на этом шаге не изменялись.
- Следующий шаг: перевести `/quantocode/` с Ghost-разметки на чистый semantic HTML.

## 25.08.2026 - Коррекция границ миграции продуктовых страниц

- Неутверждённая продуктовая дизайн-система удалена из `assets/css/site.css`.
- HTML, контент, визуал, аналитика и CTA продуктовых страниц не изменялись.
- Следующий шаг: перенести существующие стили `/quantocode/`, `/oracle/` и `/elyor/` в единый `site.css` без изменения дизайна и разметки.
- `legacy-product.css` сохраняется до завершения переноса и визуального QA всех трёх страниц.

## 26.08.2026 - Восстановление продуктового migration baseline

- Не прошедшие визуальный QA изменения `0eebe9a`, `f1f7a1e` и `0abcf49` сняты точечным восстановлением файлов из дерева `c153c54`.
- Исторические пробельные строки в восстановленных HTML признаны неблокирующими, поскольку файлы проверены на точное совпадение с подтвержденным Git-деревом.
- Соответствующее обязательное правило добавлено в `AGENTS.md`.
- `legacy-product.css`, `site.js` и исходные снимки `.work/static-baseline/` не изменялись.
- Следующий шаг: послойное удаление Ghost runtime без одновременного изменения CSS и продуктовой разметки.

## 26.08.2026 - Удаление мёртвого Ghost members workaround

- Подтверждено, что внешний Ghost Portal, Search, comments и members runtime уже отсутствует на трех продуктовых страницах.
- Из `/quantocode/`, `/oracle/` и `/elyor/` удален оставшийся inline-скрипт, предназначенный только для отключения Ghost member attribution.
- Другие script-блоки, аналитика, Telegram URL, видимый текст, inline CSS, разметка и визуальное оформление не изменялись.
- Следующий шаг: аудит Ghost-классов по фактическому использованию в `legacy-product.css` перед заменой header/footer и content shell.

## 26.08.2026 - Удаление дублирующих Ghost article headers

- Из `/quantocode/` и `/elyor/` удалены дублирующие Ghost article headers с повторными заголовками и excerpt.
- На `/oracle/` такого блока не было, поэтому страница не изменялась.
- На каждой продуктовой странице оставлен ровно один продуктовый `h1`.
- Содержимое `qk-wrap`, `oracle-page-wrapper` и `el-wrap`, CSS, JavaScript, SEO, аналитика и Telegram URL не изменялись.
- Ghost content shell пока сохранен, поскольку его CSS-контекст необходимо переносить отдельно без визуальной регрессии.
- Следующий шаг: безопасная замена классов header/footer с одновременным переносом только фактически необходимых CSS-правил.

## 26.08.2026 - Объединение локальных Inter font-face с product CSS

- Локальные `@font-face` из `assets/css/inter.css` добавлены в уже подключенный `legacy-product.css` без изменения остальных CSS-правил.
- Из `/quantocode/`, `/oracle/` и `/elyor/` удалено отдельное подключение `inter.css`.
- HTML, кроме удаленных link-тегов, inline CSS, JavaScript и продуктовые wrapper не изменялись.
- Файл `inter.css` физически сохранен до финального визуального QA и проверки отсутствия ссылок со всего сайта.
- Следующий шаг: визуальный QA шрифтов, затем перенос product CSS в отдельный от главной финальный bundle без снятия Ghost content shell.

## 26.08.2026 - Сохранение существующих html-классов product pages

- На трех продуктовых страницах destructive-присваивание `document.documentElement.className` заменено на `classList.add`.
- Текущий визуальный класс `has-dark-text` или `has-light-text` сохраняется, но последующие уникальные классы страниц больше не будут удаляться runtime-кодом.
- CSS, видимый текст, аналитика и продуктовая разметка не изменялись.
- Следующий шаг: добавить стабильные уникальные классы страниц и использовать их для безопасной изоляции переносимых inline CSS-правил.

## 26.08.2026 - Полная пересборка оболочки продуктовых страниц

- Три продуктовые страницы получили чистую статическую body-оболочку без Ghost navigation, footer и внешних Ghost-контейнеров.
- Содержимое qk-wrap, oracle-page-wrapper и el-wrap сохранено и перенесено непосредственно в main.
- Добавлен общий статический header с логотипом, продуктовой Telegram-кнопкой и визуальными вариантами для трех продуктов.
- Страницы переведены с legacy-product.css на products.css.
- SEO, аналитика, существующие продуктовые CTA и внутреннее содержимое wrapper сохранены.
- Проведён локальный визуальный QA трех страниц; header скорректирован по опубликованному визуальному baseline.
- Следующий шаг: удалить из products.css неиспользуемые Ghost-селекторы и перенести оставшиеся необходимые inline CSS-правила без изменения внешнего вида.
## 26.08.2026 - Удаление Ghost theme CSS из product bundle

- Из products.css удален неиспользуемый CSS bundle темы Ghost Source.
- Сохранены минимальный reset, базовые переменные, локальные Inter font-face и подтвержденные стили чистой product shell.
- HTML, SEO, аналитика, Telegram CTA, wrapper-разметка и embedded product CSS не изменялись.
- Следующий шаг: локальный визуальный QA, затем перенос embedded product CSS в products.css.

## 27.08.2026 - CSS продуктовых страниц вынесен из HTML

- Четыре inline style-блока каждой продуктовой страницы перенесены во внешние файлы `product-quantocode.css`, `product-oracle.css` и `product-elyor.css`.
- Общий reset и проверенный header остаются в `assets/css/products.css`.
- Исправлен общий селектор `*`, `*::before`, `*::after`.
- DOM, header, main-wrapper, JavaScript, аналитика и Telegram-ссылки не изменялись.
- Локальный визуальный QA `/quantocode/`, `/oracle/` и `/elyor/` пройден.

## 27.08.2026 - Удалены Ghost-селекторы из product CSS

- Из `product-quantocode.css`, `product-oracle.css` и `product-elyor.css` удалены селекторы `.gh-*` и Ghost-only правила.
- В смешанных групповых правилах сохранены действующие не-Ghost селекторы.
- HTML, общий `products.css`, header, main-wrapper, JavaScript и ссылки не изменялись.
- После очистки выполнен локальный визуальный QA всех трёх продуктовых страниц; изменений UI не обнаружено.

## 27.08.2026 - Удалён мёртвый JavaScript Ghost-оболочки

- Из трёх продуктовых страниц удалён неиспользуемый расчёт Ghost-классов контрастности.
- Удалён скрипт, создававший CTA внутри отсутствующей Ghost-навигации.
- Удалён дублирующий общий обработчик Quantocode CTA и отсутствующего блока pricing.
- Продуктовые обработчики CTA, аналитика, HTML, CSS, header и видимый контент сохранены.
- Следующий шаг: удалить оставшиеся обращения JavaScript к отсутствующей Ghost-навигации, сохранив продуктовый CTA tracking.
