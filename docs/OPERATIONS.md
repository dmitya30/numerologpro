# Журнал операций

Этот журнал фиксирует существенные локальные, GitHub, deployment, DNS и VPS-операции. Секреты, токены, SSH-ключи и приватные инфраструктурные данные сюда не записываются.

## 23.08.2026 - Создание локального репозитория

- Создан каталог `/c/Users/SuperSonic/projects/numerologpro`.
- Инициализирован Git-репозиторий.
- Основной веткой назначена `main`.
- GitHub CLI в локальной среде не обнаружен.
- До создания файлов рабочее дерево было пустым.

Результат: PASS.

## 23.08.2026 - Создание операционного контракта

- Создан и проверен `AGENTS.md`.
- Проверены UTF-8, LF и окончание файла ровно одним переводом строки.
- `git diff --check` прошёл.
- Пользователь подтвердил начало и конец файла.
- Создан root commit `af775211841937ddfa5244262022d7c1d9776f6c` с сообщением `docs: establish project execution contract`.

Результат: PASS.

## 23.08.2026 - Создание GitHub-репозитория

- Пользователь создал пустой публичный репозиторий `https://github.com/dmitya30/numerologpro`.
- Локальный `origin` настроен на `https://github.com/dmitya30/numerologpro.git`.
- Ветка `main` опубликована.
- Локальный и удалённый `main` подтверждены на commit `af775211841937ddfa5244262022d7c1d9776f6c`.

Результат: PASS.

## Текущее ограничение

GitHub Pages не настраивался. DNS, VPS и Ghost не изменялись. Статические страницы и ресурсы ещё не переносились.

## 23.08.2026 - Подтверждение базовой документации

- Созданы `docs/STATUS.md`, `docs/DECISIONS.md`, `docs/TODO.md` и `docs/OPERATIONS.md`.
- Проверены UTF-8, LF, окончания файлов и `git diff --check`.
- Пользователь подтвердил содержание всех четырёх документов.
- Создан коммит `d75faaa7b4e4a73c193768f9b4e5da02c1b931a7` с сообщением `docs: record migration baseline`.
- Локальный и удалённый `main` были синхронизированы.

Результат: PASS.

## 23.08.2026 - Диагностика получения исходных страниц

- Первоначальный запуск Python `urllib` получил ошибку проверки сертификата.
- Отдельная диагностика подтвердила корректные системное время, сертификат сервера и цепочку OpenSSL.
- `curl` с системной TLS-проверкой получил HTTP 200 без использования `--insecure` или `-k`.
- VPS, Ghost и TLS-настройки production не изменялись.

Результат: PASS.

## 23.08.2026 - Read-only аудит исходных страниц

- Через `curl` получены временные HTML-снимки `/`, `/elyor/`, `/quantocode/`, `/oracle/` и `/about/`.
- Создан временный отчёт `.work/source-audit/audit.json`.
- Проверены HTTP-статусы, размеры файлов и соответствие снимков отчёту.
- Пользователь выполнил визуальный QA снимков.
- `/about/` не включена автоматически в обязательный scope.
- Временные файлы находятся в игнорируемом каталоге `.work/`.
- DNS, VPS и Ghost не изменялись.

Результат: PASS.

## 23.08.2026 - Фиксация инструмента исходного аудита

- Добавлен `.gitignore` для `.work/`, `__pycache__/` и `*.py[cod]`.
- Добавлен воспроизводимый инструмент `scripts/audit-source.py`.
- Создан коммит `2ce304d501b55c7cb4fa6e7a34dbaa2df5028e79` с сообщением `chore: add source audit tooling`.
- Локальный и удалённый `main` подтверждены на одном commit.
- Рабочее дерево после публикации было чистым.

Результат: PASS.

## 24.08.2026 - Подготовка отслеживаемого контекста миграции

- На основе локального `.work/source-audit/audit.json` сформирован санитизированный manifest `migration-audit/source-pages.json`.
- Сформирована человекочитаемая сводка `migration-audit/source-pages.md`.
- Сырые HTML-снимки оставлены в игнорируемой `.work/` и не добавлены в публичную историю.
- Выполнена проверка manifest на типовые признаки ключей, authorization headers, cookies, паролей и private keys.
- В `AGENTS.md` и `DECISIONS.md` зафиксированы правила отслеживаемого временного контекста.

Результат: PASS.

## 24.08.2026 - Стабилизация статического migration baseline

- Подтверждена публикация GitHub Pages project preview.
- Исправлено расширение PNG favicon и обновлены его HTML- и Schema.org-ссылки.
- В репозиторий добавлены подтверждённые SEO-изображения, необходимые после DNS cutover.
- Удалены встроенные Ghost members styles; аналитика, canonical и видимый контент сохранены.
- Добавлены `robots.txt` и `sitemap.xml` для четырёх обязательных production URL.
- Пользовательская `404.html` оставлена на отдельный минимальный контентный и визуальный гейт.

Результат: PASS.
